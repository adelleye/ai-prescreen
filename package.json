{
  "name": "juno-quick-screen",
  "private": true,
  "version": "0.1.0",
  "description": "Juno Quick Screen — Adaptive AI Pre-Screen (Email → Web)",
  "packageManager": "pnpm@9.0.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm -r --parallel dev",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test",
    "format": "pnpm -r format",
    "prepare": "husky install"
  },
  "engines": {
    "node": ">=18.20.0"
  },
  "license": "MIT",
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "6.21.0",
    "@typescript-eslint/parser": "6.21.0",
    "@vitest/ui": "^4.0.9",
    "eslint": "8.57.0",
    "eslint-config-prettier": "9.1.0",
    "eslint-plugin-import": "2.29.1",
    "husky": "9.1.3",
    "lint-staged": "15.2.10",
    "prettier": "3.3.3",
    "vitest": "^4.0.9"
  },
  "lint-staged": {
    "*.{ts,tsx,js,jsx,json,css,md}": [
      "prettier --write",
      "eslint --fix --max-warnings=0"
    ]
  }
}
